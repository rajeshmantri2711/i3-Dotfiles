install 
playerctl for spotify
sudo apt update
sudo apt install notify-osd for notification
sudo apt install light for brighness control also add user to the video group
install green clip wget https://github.com/erebe/greenclip/releases/latest/download/greenclip
and chmod +x greenclip
sudo mv greenclip /usr/local/bin/
sudo apt install maim for screenshots
